# KGL MongoDB Integration - System Overview

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                     FRONTEND (Browser)                           │
│  ┌──────────────┐  ┌──────────────┐  ┌────────────────────────┐ │
│  │  index.html  │  │ accounts.html │  │ Other Modules (.html) │ │
│  └──────────────┘  └──────────────┘  └────────────────────────┘ │
│         ↓                ↓                        ↓               │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │           JavaScript Application Layer                        │ │
│  │  ┌─────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐  │ │
│  │  │ app.js  │  │ stock.js │  │ tasks.js │  │ invoices.js  │  │ │
│  │  │ (Login) │  │ (Stores) │  │ (Tasks)  │  │ (Invoices)   │  │ │
│  │  └─────────┘  └──────────┘  └──────────┘  └──────────────┘  │ │
│  │       ↓           ↓              ↓             ↓              │ │
│  │  ┌───────────────────────────────────────────────────────┐  │ │
│  │  │   Fetch API (HTTP Requests to Backend)               │  │ │
│  │  └───────────────────────────────────────────────────────┘  │ │
│  └──────────────────────────────────────────────────────────────┘ │
└─────────────────────┬──────────────────────────────────────────────┘
                      │ HTTP/REST API
                      │ JSON payloads
                      ↓
┌─────────────────────────────────────────────────────────────────┐
│            BACKEND (Node.js/Express Server)                     │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │             Express Application (server.js)                 │ │
│  │  ┌──────────────┬──────────────┬──────────────────────────┐ │ │
│  │  │   Routes/    │  Middleware  │    Error Handlers        │ │ │
│  │  │   Handlers   │              │                          │ │ │
│  │  └──────────────┴──────────────┴──────────────────────────┘ │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                      ↓                                             │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │           Route Layer (Backend Routes)                      │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────────────┐ │ │
│  │  │ users.js │ │ stock.js │ │ tasks.js │ │ invoices.js    │ │ │
│  │  └──────────┘ └──────────┘ └──────────┘ └────────────────┘ │ │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────────────┐ │ │
│  │  │ sales.js     │ │ transact.js   │ │ activities.js        │ │ │
│  │  └──────────────┘ └──────────────┘ └──────────────────────┘ │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                      ↓                                             │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │         Mongoose ORM Layer (Data Models)                    │ │
│  │  ┌─────────┐ ┌───────┐ ┌────────┐ ┌──────────────────────┐ │ │
│  │  │  User   │ │ Stock │ │ Invoice│ │ StockTransaction     │ │ │
│  │  └─────────┘ └───────┘ └────────┘ └──────────────────────┘ │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────────────┐ │ │
│  │  │ Task     │ │ Activity │ │ Sale     │ │ Transaction    │ │ │
│  │  └──────────┘ └──────────┘ └──────────┘ └────────────────┘ │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                      ↓                                             │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │     Database Driver (Mongoose Queries)                      │ │
│  │              ↓ SQL-like operations ↓                        │ │
│  └─────────────────────────────────────────────────────────────┘ │
└─────────────────────┬──────────────────────────────────────────────┘
                      │ MongoDB Protocol
                      │ Binary JSON (BSON)
                      ↓
┌─────────────────────────────────────────────────────────────────┐
│         DATABASE (MongoDB Instance)                              │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │        Database: karibu-groceries                           │ │
│  │  ┌──────────────┬──────────────┬──────────────────────────┐ │ │
│  │  │ users        │ stocks       │ invoices                 │ │ │
│  │  │              │              │                          │ │ │
│  │  │ ┌──────────┐ │ ┌──────────┐ │ ┌────────────────────┐  │ │ │
│  │  │ │_id       │ │ │_id       │ │ │_id                 │  │ │ │
│  │  │ │username  │ │ │itemName  │ │ │invoiceNumber       │  │ │ │
│  │  │ │role      │ │ │quantity  │ │ │clientName          │  │ │ │
│  │  │ │branch    │ │ │category  │ │ │items[]             │  │ │ │
│  │  │ └──────────┘ │ └──────────┘ │ │total               │  │ │ │
│  │  │              │              │ │status              │  │ │ │
│  │  └──────────────┴──────────────┴──────────────────────┘ │ │ │
│  │  ┌──────────────┬──────────────┬──────────────────────┐ │ │ │
│  │  │ tasks        │ activities   │ sales                │ │ │ │
│  │  │              │              │                      │ │ │ │
│  │  │ ┌──────────┐ │ ┌──────────┐ │ ┌────────────────┐  │ │ │ │
│  │  │ │_id       │ │ │_id       │ │ │_id             │  │ │ │ │
│  │  │ │title     │ │ │action    │ │ │clientName      │  │ │ │ │
│  │  │ │priority  │ │ │user      │ │ │productName     │  │ │ │ │
│  │  │ │status    │ │ │module    │ │ │quantity        │  │ │ │ │
│  │  │ │dueDate   │ │ │data      │ │ │total           │  │ │ │ │
│  │  │ └──────────┘ │ └──────────┘ │ └────────────────┘  │ │ │ │
│  │  └──────────────┴──────────────┴──────────────────────┘ │ │ │
│  │  ┌──────────────────────────────────────────────────────┐ │ │
│  │  │ transactions + stocktransactions (2 more)            │ │ │
│  │  └──────────────────────────────────────────────────────┘ │ │
│  │                                                            │ │
│  │  📊 Total Collections: 8                                  │ │
│  │  📈 Total Documents: 1000+ across all collections         │ │
│  └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow Example: Login

```
1. USER ENTERS CREDENTIALS
   ↓
   index.html (form)
   ↓
2. FRONTEND PROCESSES
   app.js → handleLogin()
   ↓
   fetch('http://localhost:5000/api/users/login', {
     method: 'POST',
     body: { username, password }
   })
   ↓
3. NETWORK REQUEST
   HTTP POST → /api/users/login
   ↓
4. BACKEND PROCESSING
   server.js → routes registered
   ↓
   users.js route → router.post('/login', ...)
   ↓
   User.findOne({ username, password })
   ↓
5. DATABASE QUERY
   MongoDB query on users collection
   ↓
   db.users.findOne({ username: 'admin', password: 'admin' })
   ↓
6. DATABASE RESPONSE
   { _id: ..., username: 'admin', role: 'admin', branch: 'Admin' }
   ↓
7. BACKEND RESPONSE
   res.json({ _id, username, role, branch })
   ↓
8. FRONTEND PROCESSES RESPONSE
   app.js → receive user data
   ↓
   localStorage.setItem('currentUser', JSON.stringify(user))
   ↓
   updateProfileUI()
   showDashboard()
   ↓
9. UI UPDATES
   Display user profile
   Show dashboard content
```

---

## 🔄 Request/Response Cycle

### Example: Create a Sale

**REQUEST:**

```
POST /api/sales HTTP/1.1
Host: localhost:5000
Content-Type: application/json

{
  "clientName": "John Doe",
  "productName": "Tomatoes",
  "quantity": 10,
  "unitPrice": 50,
  "total": 500,
  "paymentMethod": "cash"
}
```

**BACKEND PROCESSING:**

```
1. Route: POST /api/sales
2. Validation: Check required fields
3. Create: new Sale(req.body)
4. Save: await sale.save()
5. Add timestamps: createdAt, updatedAt added by Mongoose
6. Log activity: Create activity record
```

**DATABASE OPERATION:**

```
db.sales.insertOne({
  saleId: "generated-uuid",
  clientName: "John Doe",
  productName: "Tomatoes",
  quantity: 10,
  unitPrice: 50,
  total: 500,
  paymentMethod: "cash",
  status: "completed",
  createdAt: ISODate("2025-12-12T..."),
  updatedAt: ISODate("2025-12-12T...")
})
```

**RESPONSE:**

```
HTTP/1.1 201 Created
Content-Type: application/json

{
  "_id": "507f1f77bcf86cd799439011",
  "saleId": "generated-uuid",
  "clientName": "John Doe",
  "productName": "Tomatoes",
  "quantity": 10,
  "unitPrice": 50,
  "total": 500,
  "paymentMethod": "cash",
  "status": "completed",
  "createdAt": "2025-12-12T10:30:00.000Z",
  "updatedAt": "2025-12-12T10:30:00.000Z"
}
```

**FRONTEND UPDATE:**

```
1. Receive response
2. Parse JSON
3. Update in-memory sales array
4. Log activity: "Sale created"
5. Re-render sales table
```

---

## 🔑 Key Entities & Relationships

```
┌────────────────────────────────────────────────────────┐
│                     USERS                              │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ _id, username, password, role, branch, email        │ │
│ │ Roles: admin, manager, attendant, cashier           │ │
│ └─────────────────────────────────────────────────────┘ │
└────────────────────┬─────────────────────────────────────┘
                     │ created by
                     ↓
    ┌────────────────────────────────────┐
    │        TRANSACTIONS/SALES/TASKS     │
    │ All have "user" field linking to    │
    │ user who created/performed action   │
    └────────────────────────────────────┘
                     │
                     ├─────────────────────────────────┐
                     ↓                                 ↓
    ┌──────────────────────────┐    ┌──────────────────────────┐
    │      TRANSACTIONS        │    │      INVOICES            │
    │ type: sale/expense/      │    │ Contains items[]         │
    │ procurement/payment      │    │ Creates transactions     │
    │ When: Sales, Invoices    │    │ when marked paid         │
    └──────────────────────────┘    └──────────────────────────┘
            ↑                                  ↑
            │                                  │
            └──────────────────────────────────┘
                     Source
```

---

## 🚀 Module Status Heatmap

```
FRONTEND MODULES INTEGRATION STATUS:
┌─────────────────────────────────────────────────────────┐
│ Module          │ Status     │ Integration │ File        │
├─────────────────────────────────────────────────────────┤
│ Login/Dashboard │ ✅ 80%     │ 🟢 HIGH     │ app.js      │
│ Stock Mgmt      │ ✅ 100%    │ 🟢 FULL     │ stock.js    │
│ Accounts        │ 🔄 40%     │ 🟡 PARTIAL  │ accounts.js │
│ Sales Tracking  │ 🔄 30%     │ 🟡 PARTIAL  │ sales.js    │
│ Invoices        │ 🔄 20%     │ 🟡 PENDING  │ invoices.js │
│ Tasks           │ ❌ 10%     │ 🔴 PENDING  │ tasks.js    │
│ Reports         │ ❌ 5%      │ 🔴 PENDING  │ reports.js  │
│ Users           │ ❌ 5%      │ 🔴 PENDING  │ users.js    │
│ Communication   │ ❌ 0%      │ 🔴 TODO     │ comm.js     │
│ Support         │ ❌ 0%      │ 🔴 TODO     │ support.js  │
└─────────────────────────────────────────────────────────┘

Legend:
✅ = Implemented    🟢 = API Ready
🔄 = In Progress   🟡 = Partially Ready
❌ = Not Started   🔴 = Needs Implementation
```

---

## 📈 API Endpoint Coverage

```
USERS API (10 endpoints)
├─ ✅ POST   /users/login
├─ ✅ POST   /users/seed
├─ ✅ GET    /users
├─ ✅ POST   /users
├─ ✅ PUT    /users/:id
└─ ✅ DELETE /users/:id

STOCK API (10 endpoints) ⭐ FULLY IMPLEMENTED
├─ ✅ GET    /stock
├─ ✅ POST   /stock
├─ ✅ PUT    /stock/:id
├─ ✅ DELETE /stock/:id
├─ ✅ POST   /stock/:id/stock-in
├─ ✅ POST   /stock/:id/stock-out
├─ ✅ GET    /stock/alerts/low-stock
├─ ✅ GET    /stock/transactions/all
└─ ✅ GET    /stock/transactions/:id

TRANSACTIONS API (8 endpoints)
├─ ✅ GET    /transactions
├─ ✅ GET    /transactions/type/:type
├─ ✅ GET    /transactions/summary/totals
├─ ✅ POST   /transactions
├─ ✅ PUT    /transactions/:id
└─ ✅ DELETE /transactions/:id

INVOICES API (8 endpoints)
├─ ✅ GET    /invoices
├─ ✅ GET    /invoices/:id
├─ ✅ POST   /invoices
├─ ✅ PUT    /invoices/:id
├─ ✅ POST   /invoices/:id/mark-paid
└─ ✅ DELETE /invoices/:id

TASKS API (8 endpoints)
├─ ✅ GET    /tasks
├─ ✅ GET    /tasks/status/:status
├─ ✅ POST   /tasks
├─ ✅ PUT    /tasks/:id
├─ ✅ POST   /tasks/:id/complete
└─ ✅ DELETE /tasks/:id

SALES API (8 endpoints)
├─ ✅ GET    /sales
├─ ✅ GET    /sales/summary/totals
├─ ✅ POST   /sales
├─ ✅ PUT    /sales/:id
└─ ✅ DELETE /sales/:id

ACTIVITIES API (7 endpoints)
├─ ✅ GET    /activities
├─ ✅ GET    /activities/user/:username
├─ ✅ GET    /activities/module/:module
├─ ✅ POST   /activities
├─ ✅ DELETE /activities/:id
└─ ✅ DELETE /activities (clear all)

TOTAL: 59 endpoints across 7 API modules
```

---

## 💻 Command Reference

```
# Terminal 1: Start MongoDB
mongod

# Terminal 2: Start Backend
cd backend
npm run dev
# Runs on http://localhost:5000

# Terminal 3: Start Frontend
python -m http.server 8000
# Runs on http://localhost:8000

# Or use provided batch file:
start-kgl.bat
```

---

## 🎯 Implementation Timeline

```
PHASE 1: Backend Infrastructure ✅ COMPLETE
├─ MongoDB setup
├─ Express server
├─ Mongoose models (8)
├─ API routes (7 modules)
└─ Route registration

PHASE 2: Core Frontend Integration 🔄 IN PROGRESS
├─ Login system ✅ DONE
├─ Stock module ✅ DONE
├─ Accounts module 🔄 IN PROGRESS
├─ Sales module 🔄 QUEUED
├─ Invoices module ⏳ QUEUED
└─ Tasks module ⏳ QUEUED

PHASE 3: Security & Validation ⏳ NOT STARTED
├─ JWT authentication
├─ Password hashing
├─ RBAC middleware
└─ Input validation

PHASE 4: Testing & Deployment ⏳ NOT STARTED
├─ Unit tests
├─ Integration tests
├─ Performance tests
└─ Production deployment

TIME ESTIMATE:
Phase 1: ✅ 8 hours
Phase 2: 🔄 ~12-16 hours remaining
Phase 3: ⏳ ~8-10 hours
Phase 4: ⏳ ~6-8 hours
─────────────
TOTAL: ~34-42 hours
```

---

**Generated:** 2025-12-12  
**Status:** Active Development  
**Last Update:** Backend Complete, Frontend Integration 30% Complete
